/**
 * TODO's
 *
 *
 * Limit Category and Price for 2 Decimal values
 * Proper scroll functionality in Row Container Component
 * Fix lag during toggle cart menu

 *
 * MAYBE's
 *
 * Store orders according to unique user emails
 * Proper logout rather that only clearing local storage
 *
 * FIXED
 *
 * Use UUID in place of Date.now
 * Local storage getting cleared resulting in undefined logout
 * Add Netlify for Google Auth
 */
